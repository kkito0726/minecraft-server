# usecase

ユースケース（アプリケーションサービス）を置く。

手順の**順序**に責任を持つ層。たとえば復元なら「確認 → 空き容量 → 停止 → 退避 →
展開 → 設定更新 → 起動」の順序そのものがここの関心事であり、
その順序が崩れると壊れたワールドができる。

外部とは [`../port`](../port) のインターフェース越しにしか話さない。
そのため、テストは呼び出しを記録する偽物を差し込むだけでよく、
実際の docker も Minecraft サーバーも必要にならない。

フェーズ 9 以降で実装する。
