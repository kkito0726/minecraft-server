// ホスト（Pi 本体）の資源の使用状況。

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: mcadmin/v1/system.proto

package mcadminv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/kkito0726/minecraft-server/backend/gen/mcadmin/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SystemServiceName is the fully-qualified name of the SystemService service.
	SystemServiceName = "mcadmin.v1.SystemService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SystemServiceGetMetricsProcedure is the fully-qualified name of the SystemService's GetMetrics
	// RPC.
	SystemServiceGetMetricsProcedure = "/mcadmin.v1.SystemService/GetMetrics"
)

// SystemServiceClient is a client for the mcadmin.v1.SystemService service.
type SystemServiceClient interface {
	// GetMetrics はホストの CPU・メモリ・ストレージの使用状況を返す。
	//
	// コンテナではなくホストを見る。ストレージはコンテナと共有であり、
	// 空き容量はそのままサーバーの生死に関わるため、境界はホストに置く。
	//
	// 読めない項目があっても RPC は失敗させない。項目ごとの available を
	// 偽にして理由を添える。失敗させると、資源の状況を見るための画面が
	// まさに資源が苦しいときに開けなくなる（GetGameSettings と同じ方針）。
	GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error)
}

// NewSystemServiceClient constructs a client for the mcadmin.v1.SystemService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSystemServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SystemServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	systemServiceMethods := v1.File_mcadmin_v1_system_proto.Services().ByName("SystemService").Methods()
	return &systemServiceClient{
		getMetrics: connect.NewClient[v1.GetMetricsRequest, v1.GetMetricsResponse](
			httpClient,
			baseURL+SystemServiceGetMetricsProcedure,
			connect.WithSchema(systemServiceMethods.ByName("GetMetrics")),
			connect.WithClientOptions(opts...),
		),
	}
}

// systemServiceClient implements SystemServiceClient.
type systemServiceClient struct {
	getMetrics *connect.Client[v1.GetMetricsRequest, v1.GetMetricsResponse]
}

// GetMetrics calls mcadmin.v1.SystemService.GetMetrics.
func (c *systemServiceClient) GetMetrics(ctx context.Context, req *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error) {
	return c.getMetrics.CallUnary(ctx, req)
}

// SystemServiceHandler is an implementation of the mcadmin.v1.SystemService service.
type SystemServiceHandler interface {
	// GetMetrics はホストの CPU・メモリ・ストレージの使用状況を返す。
	//
	// コンテナではなくホストを見る。ストレージはコンテナと共有であり、
	// 空き容量はそのままサーバーの生死に関わるため、境界はホストに置く。
	//
	// 読めない項目があっても RPC は失敗させない。項目ごとの available を
	// 偽にして理由を添える。失敗させると、資源の状況を見るための画面が
	// まさに資源が苦しいときに開けなくなる（GetGameSettings と同じ方針）。
	GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error)
}

// NewSystemServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSystemServiceHandler(svc SystemServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	systemServiceMethods := v1.File_mcadmin_v1_system_proto.Services().ByName("SystemService").Methods()
	systemServiceGetMetricsHandler := connect.NewUnaryHandler(
		SystemServiceGetMetricsProcedure,
		svc.GetMetrics,
		connect.WithSchema(systemServiceMethods.ByName("GetMetrics")),
		connect.WithHandlerOptions(opts...),
	)
	return "/mcadmin.v1.SystemService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SystemServiceGetMetricsProcedure:
			systemServiceGetMetricsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSystemServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSystemServiceHandler struct{}

func (UnimplementedSystemServiceHandler) GetMetrics(context.Context, *connect.Request[v1.GetMetricsRequest]) (*connect.Response[v1.GetMetricsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("mcadmin.v1.SystemService.GetMetrics is not implemented"))
}
